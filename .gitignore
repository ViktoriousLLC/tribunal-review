node_modules/
.DS_Store
*.log

# npm pack output — a build artifact, never a source file.
*.tgz

# Mutation testing: the sandbox Stryker copies the project into, and its report.
# Both are regenerated by every run.
.stryker-tmp/
reports/
